class Game {
    constructor(playerAsWhite) {
        this.playerAsWhite = playerAsWhite // bool;
    }

    initStartingBoard() {
        // Initialize board orientation for the current player.

        if (this.playerAsWhite) {
            // orient board for white
        } else {
            // orient board for black
        }
    }

    movePiece() {
        // pieceId : string
        // to [x, y]
        
    }
}